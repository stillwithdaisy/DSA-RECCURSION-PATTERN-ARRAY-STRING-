#include <iostream>
using namespace std;

void printNos(int n){
    
    // base condition
    if (n == 0)
        return;
        
    // recursive call
    printNos(n - 1);
    cout << n << " ";
}

int main(){
    int n = 3;
    printNos(n);
    return 0;
}